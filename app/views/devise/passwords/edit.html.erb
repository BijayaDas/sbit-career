<div class="">
        <div id="wrapper">
            <div class="animate form">
                <section class="login_content">
                    <%= form_for(resource, as: resource_name, url: password_path(resource_name), html: { method: :put }) do |f| %>
                    <%= devise_error_messages! %>
                    <%= f.hidden_field :reset_password_token %>
                        <h1>Forgot password</h1>
                        <div>
                        <% if @minimum_password_length %>
                          <em>(<%= @minimum_password_length %> characters minimum)</em><br />
                        <% end %><br/>
                        <%= f.email_field :password, autofocus: false,class: "form-control", placeholder: "Email" %>
                        </div>
                         <div>
                        <%= f.email_field :password_confirmation, autofocus: false,class: "form-control", placeholder: "Email" %>
                        </div>

                        <div>
                        <%= f.submit "Change my password", class: "btn btn-primary submit pull-left" %>
                    
                          <%= link_to "login", new_user_session_path,class: "reset_pass pull-right" %>
                           
                        </div>
                        <br/>
                            <div class="clearfix"></div>
                            <div class="clearfix"></div>
                        
                        <div class="separator">
                            <div>
                                <h1>Sourcebits Inc.</h1>

                                <p>©2015 All Rights Reserved.</p>
                            </div>
                        </div>
                    <% end %>
                    <!-- form -->
                </section>
                <!-- content -->
            </div>
        </div>
    </div>
