
<% flash.each do |type, message| %>
  <div class="alert fade in <%= bootstrap_class_for(type.to_sym) %> alert-dismissable">
    <button class="close" data-dismiss="alert">×</button>
    <%= message %>
  </div>
<% end %>